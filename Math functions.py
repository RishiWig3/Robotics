import math

def areaCircle(radius):
    Circlearea = float(math.pi * (radius**2))
    return Circlearea

def volumeCylinder(radius, height):
    CylinderVolume = float(((math.pi * (radius**2)) * height))
    return CylinderVolume

def areaTriangle(base, height):
    TriangleArea = float((base * height) * 0.5)
    return TriangleArea

def areaSquare(side):
    SquareArea = float(side**2)
    return SquareArea

def volumeCube(side):
    CubeVolume = float(side**3)
    return CubeVolume

def rectangleArea(length, width):
    AreaRectangle = float(length * width)
    return AreaRectangle

def RectangularPrismVolume(length, width, breadth):
    VolumeRectangularPrism = float(length*width*breadth)
    return VolumeRectangularPrism

def Quadratic(a, b, c):
    QuadraticEquation2 = float(((-b) - (((b**2)-4*a*c)**0.5)/(2*a))
    return QuadraticEquation2
    




















                                      




    

